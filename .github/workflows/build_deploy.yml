name: Build and Deploy

on:
  push:

jobs:
  checkout-demo:
    runs-on: ubuntu-latest
    env:
      MY_NAME: Tanner

    steps:
      - name: Print variable
        run: echo "Hello $MY_NAME"

      - name:  Checkout repo
        uses: actions/checkout@v3

      - name:  List files
        run: ls -la
